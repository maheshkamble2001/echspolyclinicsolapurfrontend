import { AcademicCapIcon, CurrencyDollarIcon, Squares2X2Icon } from '@heroicons/react/24/outline';
import { NAV_TYPE_ROOT, NAV_TYPE_ITEM } from 'constants/app.constant'
import { ChartBarIcon } from 'lucide-react';


const ROOT_DASHBOARDS = '/categorymanagement';

const path = (root, item) => `${root}${item}`;

export const categorymanagement = {
    // id: 'categorymanagement',
    // type: NAV_TYPE_ROOT,
    // role: 400000,
    // path: '/categorymanagement/expensecategory',
    // title: 'Ledger Management',
    // Icon: Squares2X2Icon ,
   
}

